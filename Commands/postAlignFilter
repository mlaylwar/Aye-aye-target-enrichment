#Select reads witout clipping based on CIGAR
samtools view -h $k{k}_WG_MarkDup.bam | awk '$6 !~ /H|S/{print}' | samtools view -bS - > ${k}.noclips.bam

#Select reads with mapping quality of 60
samtools view -bq 60 ${k}.noclips.bam > ${k}_QF.bam
